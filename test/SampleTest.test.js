const { assert } = require('chai')

require('chai')
    .use(require('chai-as-promised'))
    .should()

contract('Sample', () => {

    before(async () => {
        
    })

    describe('Describe test', async () => {
        it('Test title', async () => {
            
        })
    })
})
    